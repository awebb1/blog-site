---
layout: base.njk
title: Blog
description: Posts about hacking, coding, gaming, and whatever else.
---
<link rel="stylesheet" href="/css/blog.css">

<section class="blog-page">
  <div class="blog-page__inner">
    <p class="blog-page__label">// blog</p>
    <h1 class="blog-page__title">Posts</h1>
    <p class="blog-page__desc">Thoughts on security, code, games, and the occasional rant.</p>

    {% if collections.tagList.length %}
    <div class="tag-filter">
      <a href="/blog/" class="tag tag--active">all</a>
      {%- for tag in collections.tagList -%}
      <a href="/blog/tags/{{ tag }}/" class="tag">{{ tag }}</a>
      {%- endfor -%}
    </div>
    {% endif %}

    {% set postList = collections.posts %}
    {% if postList.length %}
    <div class="posts-grid">
      {%- for post in postList -%}
      <div class="post-card">
        <p class="post-card__date">{{ post.date | readableDate }}</p>
        <h2 class="post-card__title"><a href="{{ post.url }}">{{ post.data.title }}</a></h2>
        {% if post.data.excerpt %}
        <p class="post-card__excerpt">{{ post.data.excerpt }}</p>
        {% endif %}
        {% if post.data.tags %}
        <div class="post-card__tags">
          {%- for tag in post.data.tags -%}
            {%- if tag != "post" -%}
            <a href="/blog/tags/{{ tag }}/" class="tag">{{ tag }}</a>
            {%- endif -%}
          {%- endfor -%}
        </div>
        {% endif %}
        <a href="{{ post.url }}" class="post-card__read">Read more &rarr;</a>
      </div>
      {%- endfor -%}
    </div>
    {% else %}
    <div class="posts-empty">
      <p>No posts yet. Check back soon.</p>
    </div>
    {% endif %}
  </div>
</section>
