---
layout: base.njk
title: Pentester. Coder. Gamer.
---
{% block head %}
<style>
.hero {
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: var(--space-3xl) var(--space-md) var(--space-2xl);
  position: relative;
  overflow: hidden;
}
.hero::before {
  content: '';
  position: absolute;
  top: -50%;
  right: -20%;
  width: 600px;
  height: 600px;
  background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
  border-radius: 50%;
  pointer-events: none;
}
.hero::after {
  content: '';
  position: absolute;
  bottom: -30%;
  left: -10%;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, var(--accent-glow) 0%, transparent 70%);
  border-radius: 50%;
  pointer-events: none;
}
.hero__inner {
  max-width: var(--max-width);
  width: 100%;
  position: relative;
  z-index: 1;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-xl);
  align-items: center;
}
.hero__content { width: 100%; }
.hero__image { position: relative; min-height: 100%; }
.hero__image img {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  border-radius: var(--radius-lg);
  mask-image: linear-gradient(to left, #000 60%, transparent 100%);
  -webkit-mask-image: linear-gradient(to left, #000 60%, transparent 100%);
}
.hero__label {
  display: inline-block;
  font-family: var(--font-mono);
  font-size: var(--fs-sm);
  color: var(--accent);
  margin-bottom: var(--space-md);
  letter-spacing: 0.05em;
}
.hero__title {
  font-size: var(--fs-hero);
  font-weight: 900;
  letter-spacing: -0.03em;
  line-height: 1.05;
  margin-bottom: var(--space-md);
}
.hero__title span {
  background: linear-gradient(135deg, var(--accent) 0%, var(--accent-hover) 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
.hero__title span::selection {
  -webkit-text-fill-color: #0f0f0f;
}
.light-mode .hero__title span::selection {
  -webkit-text-fill-color: #fff;
}
.hero__subtitle {
  font-size: var(--fs-lg);
  color: var(--text-secondary);
  max-width: 600px;
  margin-bottom: var(--space-xl);
  line-height: 1.5;
}
.hero__actions {
  display: flex;
  gap: var(--space-md);
  flex-wrap: wrap;
}
.btn {
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: 0.875rem 2rem;
  border-radius: var(--radius);
  font-size: var(--fs-sm);
  font-weight: 600;
  font-family: var(--font-body);
  cursor: pointer;
  transition: all var(--transition);
  text-decoration: none;
  border: none;
}
.btn--primary { background: var(--accent); color: #0f0f0f; }
.btn--primary:hover {
  background: var(--accent-hover);
  color: #0f0f0f;
  transform: translateY(-2px);
  box-shadow: 0 8px 24px rgba(196, 131, 155, 0.3);
}
.btn--outline {
  background: transparent;
  color: var(--text-primary);
  border: 1px solid var(--border);
}
.btn--outline:hover {
  border-color: var(--accent);
  color: var(--accent);
  transform: translateY(-2px);
}
.btn__arrow { transition: transform var(--transition); }
.btn:hover .btn__arrow { transform: translateX(4px); }
.about {
  padding: var(--space-3xl) var(--space-md);
  background: var(--bg-secondary);
  transition: background-color var(--transition);
}
.about__inner {
  max-width: var(--max-width);
  margin: 0 auto;
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: var(--space-3xl);
  align-items: center;
}
.about__label {
  font-family: var(--font-mono);
  font-size: var(--fs-sm);
  color: var(--accent);
  margin-bottom: var(--space-sm);
  letter-spacing: 0.05em;
}
.about__title { font-size: var(--fs-2xl); margin-bottom: var(--space-lg); }
.about__text {
  color: var(--text-secondary);
  margin-bottom: var(--space-md);
  line-height: 1.7;
}
.about__topics {
  display: flex;
  flex-wrap: wrap;
  gap: var(--space-xs);
  margin-top: var(--space-lg);
}
.about__topic {
  padding: 0.4rem 1rem;
  background: var(--bg-tertiary);
  border: 1px solid var(--border);
  border-radius: 100px;
  font-size: var(--fs-xs);
  color: var(--text-secondary);
  font-family: var(--font-mono);
  transition: background-color var(--transition), border-color var(--transition), color var(--transition);
}
.about__visual { display: flex; justify-content: center; align-items: center; }
.about__terminal {
  background: var(--code-bg);
  border: 1px solid var(--border);
  border-radius: var(--radius-lg);
  padding: var(--space-lg);
  width: 100%;
  max-width: 480px;
  font-family: var(--font-mono);
  font-size: var(--fs-sm);
  transition: background-color var(--transition), border-color var(--transition);
}
.terminal__bar {
  display: flex;
  gap: 8px;
  margin-bottom: var(--space-md);
  padding-bottom: var(--space-sm);
  border-bottom: 1px solid var(--border);
}
.terminal__dot { width: 12px; height: 12px; border-radius: 50%; }
.terminal__dot--red { background: #ff5f57; }
.terminal__dot--yellow { background: #febc2e; }
.terminal__dot--green { background: #28c840; }
.terminal__line { margin-bottom: 0.5rem; color: var(--text-secondary); }
.terminal__line .prompt { color: var(--accent); }
.terminal__line .cmd { color: var(--text-heading); }
.terminal__cursor {
  display: inline-block;
  width: 8px;
  height: 16px;
  background: var(--accent);
  animation: blink 1s step-end infinite;
  vertical-align: text-bottom;
}
@keyframes blink { 50% { opacity: 0; } }
.contact { padding: var(--space-3xl) var(--space-md); }
.contact__inner { max-width: var(--content-width); margin: 0 auto; text-align: center; }
.contact__label {
  font-family: var(--font-mono);
  font-size: var(--fs-sm);
  color: var(--accent);
  margin-bottom: var(--space-sm);
  letter-spacing: 0.05em;
}
.contact__title { font-size: var(--fs-2xl); margin-bottom: var(--space-md); }
.contact__text { color: var(--text-secondary); margin-bottom: var(--space-xl); line-height: 1.7; }
.contact__links { display: flex; justify-content: center; gap: var(--space-md); flex-wrap: wrap; }
.contact__link {
  display: inline-flex;
  align-items: center;
  gap: var(--space-xs);
  padding: 0.75rem 1.5rem;
  border: 1px solid var(--border);
  border-radius: var(--radius);
  color: var(--text-primary);
  font-size: var(--fs-sm);
  font-weight: 500;
  transition: all var(--transition);
}
.contact__link:hover {
  border-color: var(--accent);
  color: var(--accent);
  transform: translateY(-2px);
}
@media (max-width: 768px) {
  .hero__inner { grid-template-columns: 1fr; }
  .hero__image { order: -1; }
  .hero__image img { max-height: 300px; }
  .about__inner { grid-template-columns: 1fr; gap: var(--space-xl); }
  .about__visual { order: -1; }
  .hero__actions { flex-direction: column; align-items: flex-start; }
}
</style>
{% endblock %}

<section class="hero">
  <div class="hero__inner">
    <div class="hero__content">
      <p class="hero__label">// hello world</p>
      <h1 class="hero__title">Alex <span>W.</span></h1>
      <p class="hero__subtitle">Penetration tester by trade. Coder by compulsion. Gamer by addiction. Writing about breaking things, building things, and the occasional game.</p>
      <div class="hero__actions">
        <a href="/blog/" class="btn btn--primary">
          Read the Blog <span class="btn__arrow">&rarr;</span>
        </a>
        <a href="#contact" class="btn btn--outline">
          Get in Touch
        </a>
      </div>
    </div>
    <div class="hero__image">
      <img src="/img/hero.webp" alt="Illustration of a person coding at a desk" width="768" height="512" loading="eager" fetchpriority="high">
    </div>
  </div>
</section>

<section class="about" id="about">
  <div class="about__inner">
    <div class="about__copy">
      <p class="about__label">// about me</p>
      <h2 class="about__title">A bit about me</h2>
      <p class="about__text">I'm a penetration tester who spends way too much time at their desk. When I'm not looking for vulnerabilities, I'm probably writing code (more vibe coding these days), studying something I find neat, or losing hours to a game I told myself I'd only play for an hour.</p>
      <p class="about__text">This site exists because Medium's CloudFlare WAF keeps blocking my hacking blogs. Here I write about what I've been up to, CTF writeups, coding projects, game stuff, and whatever else crosses my mind. Really just a lot of random words</p>
      <div class="about__topics">
        <span class="about__topic">pentesting</span>
        <span class="about__topic">writeups</span>
        <span class="about__topic">python</span>
        <span class="about__topic">cyber security</span>
        <span class="about__topic">linux</span>
        <span class="about__topic">games</span>
        <span class="about__topic">dev tools</span>
        <span class="about__topic">ai</span>
      </div>
    </div>
    <div class="about__visual">
      <div class="about__terminal">
        <div class="terminal__bar">
          <span class="terminal__dot terminal__dot--red"></span>
          <span class="terminal__dot terminal__dot--yellow"></span>
          <span class="terminal__dot terminal__dot--green"></span>
        </div>
        <div class="terminal__line"><span class="prompt">~$</span> <span class="cmd">whoami</span></div>
        <div class="terminal__line">alex:pentester</div>
        <div class="terminal__line"><span class="prompt">~$</span> <span class="cmd">cat /etc/motd</span></div>
        <div class="terminal__line">builder of things.</div>
        <div class="terminal__line">breaker of things.</div>
        <div class="terminal__line"><span class="prompt">~$</span> <span class="terminal__cursor"></span></div>
      </div>
    </div>
  </div>
</section>

<section class="contact" id="contact">
  <div class="contact__inner">
    <p class="contact__label">// get in touch</p>
    <h2 class="contact__title">Want to connect?</h2>
    <p class="contact__text">Whether it's about security work, a project idea, or you just want to argue about why I don't hate Overwatch — feel free to reach out.</p>
    <div class="contact__links">
      <a href="{{ site.github }}" target="_blank" rel="noopener" class="contact__link">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
        GitHub
      </a>
      <a href="{{ site.linkedin }}" target="_blank" rel="noopener" class="contact__link">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="currentColor"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
        LinkedIn
      </a>
      <a href="mailto:alex@alexdoesthings.dev" class="contact__link">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/></svg>
        Email
      </a>
    </div>
  </div>
</section>
